# 快速修复 /categories 和 /archives 404 问题

## 问题

访问 `/categories` 和 `/archives` 显示 "Cannot GET" 或 404 错误。

## 🔧 一键修复（推荐）

### Windows 用户

在 Hexo 博客根目录运行：

```bash
cd themes/photo-blog
install.bat
```

或手动执行：

```bash
npm install hexo-generator-archive --save
npm install hexo-generator-category --save
npm install hexo-generator-tag --save
hexo clean
hexo generate
hexo server
```

### Mac/Linux 用户

在 Hexo 博客根目录运行：

```bash
cd themes/photo-blog
chmod +x install.sh
./install.sh
```

或手动执行：

```bash
npm install hexo-generator-archive --save
npm install hexo-generator-category --save
npm install hexo-generator-tag --save
hexo clean
hexo generate
hexo server
```

## ✅ 验证修复

安装完成后，访问以下地址验证：

- http://localhost:4000/archives
- http://localhost:4000/categories
- http://localhost:4000/tags

## 📝 创建测试数据

如果页面显示但内容为空，创建一些测试文章：

```bash
hexo new post "我的第一篇文章"
```

编辑生成的文件，添加分类和标签：

```yaml
---
title: 我的第一篇文章
date: 2024-01-15 14:30:00
categories:
  - 生活
tags:
  - 日常
  - 测试
---

文章内容...
```

然后重新生成：

```bash
hexo clean
hexo generate
hexo server
```

## 🆘 仍然有问题？

查看详细的故障排除指南：[TROUBLESHOOTING.md](TROUBLESHOOTING.md)

## 📚 完整依赖列表

为了主题功能完整，建议安装以下所有插件：

```bash
# 必需插件
npm install hexo-renderer-ejs --save
npm install hexo-generator-archive --save
npm install hexo-generator-category --save
npm install hexo-generator-tag --save
npm install hexo-generator-index --save

# 推荐插件
npm install hexo-generator-feed --save
npm install hexo-generator-sitemap --save
npm install hexo-generator-searchdb --save
npm install hexo-deployer-git --save
```
